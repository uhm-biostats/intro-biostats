---
title: "Probability"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, warning = FALSE, message = FALSE,
                      eval = TRUE)

library(cowplot)
library(ggplot2)
library(tibble)
library(dplyr)
library(tidyr)
library(viridis)
```

## Ka hua ʻōlelo o ka haʻawina

::: {style="text-align: center;"}
[lūlū]{.alert}
:::

## Learning goals

1. Define [random trial]{.alert}, [event]{.alert}, and [outcome]{.alert}
2. Define [probability]{.alert}
3. Describe discrete and continuous [probability distributions]{.alert}
4. Determine if events are [mutually exclusive]{.alert}
5. Determine if events are [independent]{.alert}

## Define [random trial]{.alert}

- What's going to happen tomorrow?
- If I flip a coin---heads or tails?
- If I roll a die, what number comes up?
- If I measure a fish, how long is it?
- If I conduct an insect survey in the Koʻolau Mountains, how many species will I find?
- What is the effect of a new drug on a patient enrolled in a clinical trial?

These are all [random trials]{.alert}

## Define [random trial]{.alert}

> A [random trial]{.alert} is a process or experiment that has two or more possible outcomes whose occurrence cannot be predicted with certainty

![](/img/die_roll.png){width="40%" fig-align="center"}

## Define [event]{.alert} and [outcome]{.alert}

*An [event]{.alert} is one observation of a random trial*

*The [outcomes]{.alert} of a random trial describe all the possible events*

## Define [random trial]{.alert}, [event]{.alert}, and [outcome]{.alert}

::: columns
::: {.column width="34%"}
The [random trial:]{.alert}

you roll the die

![](/img/die_roll.png){width="6cm"}
:::

::: {.column width="28%"}
An [event:]{.alert}

you get a 5

![](/img/5_die.png){width="2.5cm"}
:::

::: {.column width="38%"}
All possible [outcomes:]{.alert}

all integers 1---6
:::
:::

## Define [probability]{.alert}

*The [probability]{.alert} of an event is the proportion of times that event would occur if we repeated a random trial over and over again under the same conditions. Probability always ranges between 0 and 1*

## Define [probability]{.alert}

With a fair die, every outcome has equal probability:

```{r}
library(tidydice)
library(gridExtra)

d1 <- roll_dice(times = 1, prob = c(1, 0, 0, 0, 0, 0)) |>
    plot_dice(detailed = FALSE) + labs(title = NULL)
d2 <- roll_dice(times = 1, prob = c(0, 1, 0, 0, 0, 0)) |>
    plot_dice(detailed = FALSE) + labs(title = NULL)
d3 <- roll_dice(times = 1, prob = c(0, 0, 1, 0, 0, 0)) |>
    plot_dice(detailed = FALSE) + labs(title = NULL)
d4 <- roll_dice(times = 1, prob = c(0, 0, 0, 1, 0, 0)) |>
    plot_dice(detailed = FALSE) + labs(title = NULL)
d5 <- roll_dice(times = 1, prob = c(0, 0, 0, 0, 1, 0)) |>
    plot_dice(detailed = FALSE) + labs(title = NULL)
d6 <- roll_dice(times = 1, prob = c(0, 0, 0, 0, 0, 1), success = 1) |>
    plot_dice(detailed = FALSE) + labs(title = NULL)

pr <- data.frame(rep("= 1/6", 6))
names(pr) <- NULL
rownames(pr) <- paste0("Pr(rolling a ", 1:6, ")")

grid.arrange(d1, tableGrob(pr[1, ], theme = ttheme_default(base_size = 32)),
             d2, tableGrob(pr[2, ], theme = ttheme_default(base_size = 32)),
             d3, tableGrob(pr[3, ], theme = ttheme_default(base_size = 32)),
             d4, tableGrob(pr[4, ], theme = ttheme_default(base_size = 32)),
             d5, tableGrob(pr[5, ], theme = ttheme_default(base_size = 32)),
             d6, tableGrob(pr[6, ], theme = ttheme_default(base_size = 32)),
             ncol = 2)
```

## Define [probability]{.alert}

Probability is just counting, sometimes *very complex* counting.

But with a die it is simple counting:

$Pr(rolling~a~5) = 1/6$

because there is only **1** way to roll a 5, and there are **6** possible outcomes

Probability is all the ways you could get a specific event, divided by the number of possible outcomes

## Define [probability]{.alert}

Probability is all the ways you could get a specific event, divided by the number of possible outcomes

Consider rolling **2 dice** what are all the possible outcomes?

::: {.pause-think}
Why is 7 a lucky number in many cultures?
:::

## Define [probability]{.alert}

Probability is all the ways you could get a specific event, divided by the number of possible outcomes

```{r}
#| echo: true

# all the ways to get 7 from 2 dice:
# (1, 6); (6, 1); (2, 5); (5, 2)
# (3, 4); (4, 3)
6

# number of possible outcomes of 2 dice:
6 * 6

# probability of rolling a 7:
6 / 36 
```


## Define [probability]{.alert}

Probability is all the ways you could get a specific event, divided by the number of possible outcomes


```{r}
#| echo: true

# simulate it
nroll <- 10000
crap <- replicate(nroll, {
    d1 <- sample(6, 1)
    d2 <- sample(6, 1)

    d1 + d2
})

sum(crap == 7) / nroll # this should be about 1/6
```



## Define [probability distribution]{.alert}

We could make a histogram-looking thing of probabilities of all the possible outcomes:

```{r}
#| label: dicedist
#| fig-width: 4
#| fig-height: 2
#| fig-align: center
#| out-width: "7in"
#| cache: true

ggplot(data.frame(sum_of_2_dice = crap), aes(x = sum_of_2_dice)) +
    geom_histogram(aes(y = after_stat(density)),
                   binwidth = 1, color = "black") +
    scale_x_continuous(breaks = seq(2, 12, by = 1)) +
    theme_cowplot()
```

This histogram-looking thing is called a [probability distribution]{.alert}

## Define [probability distribution]{.alert}

![](/img/2_dice_hist.png){width="70%" fig-align="center"}

This histogram-looking thing is called a [discrete probability distribution]{.alert}

## Why do we care?

Probability is everywhere!

```{=html}
<iframe width="1120" height="630" src="https://www.youtube.com/embed/zJN5W-EreVs?si=Uj8ZwwHzUeqI1X-u&start=70&end=89" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>
```

## Why do we care?

::: columns
::: {.column width="60%"}
Probability is everywhere!

*If I conduct an insect survey, how many species will I find?*

::: {.fragment style="font-size: 70%"}

| Process | Event |
|---------|-------|
| No populations establish | $S = 0$ |
| A population establishes and goes extinct | $S = 0$ |
| 2 population establish and 1 goes extinct | $S = 1$ |
| $\cdots$ | $\cdots$ |
| N populations establish, M go extinct | $S = N - M$ |
| $\cdots$ | $\cdots$ |
| N populations establish, none go extinct | $S = N$ |
| $\cdots$ | $\cdots$ |

:::

::: {.fragment}

**Complex counting!**

:::
:::

::: {.column width="40%"}
![](/img/hi_arth.png)
:::
:::



## Why do we care?

::: columns
::: {.column width="60%"}
Probability is everywhere!

*If I conduct an insect survey, how many species will I find?*

Different forces in nature, like speciation or species interactions, change the probability of different processes.  In effect, those forces **load the dice** differently
:::

::: {.column width="40%"}

:::: {.r-stack}

::::: {.fragment .fade-out}
![](/img/hi_arth.png)
:::::

::::: {.fragment}

```{r}
#| fig-width: 3
#| fig-height: 4
#| fig-align: center
#| out-width: "5in"

dat <- data.frame(y = c(rpois(100, 50), rpois(100, 80)),
                  speciation = c(rep("less speciation", 100),
                                 rep("more speciation", 100)))

ggplot(dat, aes(x = y, fill = speciation)) +
    geom_histogram(aes(y = after_stat(density)), bins = 20) +
    facet_grid(rows = vars(speciation)) +
    theme_cowplot() +
    scale_fill_viridis_d(option = "A", direction = -1, 
                         begin = 0.2, end = 0.8) +
    theme(legend.position = "none") +
    xlab("Number of species")
```

:::::



::::
:::
:::




## Why do we care?

::: columns
::: {.column width="60%"}
*Remember noni?*

::: {.r-stack}

::: {.fragment .fade-in-then-out}
Suppose we run a clinical study of the effect on noni juice on inflammatory arthritis. We randomly assign half the patients to receive 10mg noni extract and the other half to receive a placebo. We measure change in erythrocyte sedimentation rate (*sed rate*) in all patients before and after treatment (either 10mg noni or placebo)
:::

::: {.fragment .fade-in-then-out}
Noni is a crop brought to Hawaiʻi by Kānaka ʻŌiwi. The ʻike of its use comes from inter-generational kilo.

- Who gets to decide whether/how to commodify noni?
- Any clinical trial should be led by lāʻau lapaʻau practitioners and benefits governed by ʻŌiwi communities
:::

:::

:::

::: {.column width="40%"}
![](/img/noni.jpg){width="80%"}
:::
:::



## Why do we care?

::: columns
::: {.column width="60%"}

Suppose we run a clinical study of the effect of noni juice on inflammatory arthritis. We randomly assign half the patients to receive 10mg noni extract and the other half to receive a placebo. We measure change in erythrocyte sedimentation rate (*sed rate*) in all patients before and after treatment (either 10mg noni or placebo)
:::

::: {.column width="40%"}
```{r}
#| fig-width: 4
#| fig-height: 4
#| fig-align: center
#| out-width: "5in"

set.seed(1)
dat <- data.frame(sed = c(rnorm(100, -2), rnorm(100)),
                  group = c(rep("10mg noni", 100),
                            rep("placebo", 100)))

ggplot(dat, aes(x = sed, fill = group)) +
    geom_histogram(aes(y = after_stat(density)), bins = 24) +
    facet_grid(rows = vars(group)) +
    theme_cowplot() +
    theme(legend.position = "none") +
    xlab("Change in sed rate [mm/hr]\n(after - before)")
```
:::
:::


## [Continuous probability distributions]{.alert}

::: {.r-stack}

::: {.fragment .fade-in-then-out}
```{r}
#| label: sedhist
#| fig-width: 4
#| fig-height: 2
#| out-width: "5in"
#| cache: true

ggplot(dat[dat$group != "placebo", ], aes(x = sed, fill = group)) +
    geom_histogram(aes(y = after_stat(density)), bins = 23) +
    facet_grid(rows = vars(group)) +
    theme_cowplot() +
    theme(legend.position = "none") +
    xlab("Change in sed rate [mm/hr]\n(after - before)")
```

*Change in sed rate [mm/hr] is* ***not*** *an integer*

What do we do?!
:::


::: {.fragment .fade-in-then-out}
![](/img/sed_hist.png){width="80%" fig-align="center"}
:::

::: {.fragment .fade-in-then-out}

Imagine we had millions of data points, and we could draw very narrow bins

```{r}
#| fig-width: 4
#| fig-height: 2
#| out-width: "8in"
#| fig-align: center

dat <- data.frame(sed = rnorm(1000000, -2), group = "10mg noni")

ggplot(dat, aes(x = sed, fill = group)) +
    geom_histogram(aes(y = after_stat(density)), bins = 100) +
    facet_grid(rows = vars(group)) +
    theme_cowplot() +
    theme(legend.position = "none") +
    xlab("Change in sed rate [mm/hr]\n(after - before)")
```

We would get a very smooth looking distribution

:::

::: {.fragment .fade-in-then-out}

Continuous probability distributions are typically visualized as smooth curves, *without bars*

```{r}
#| label: seddens
#| fig-width: 4
#| fig-height: 2
#| out-width: "8in"
#| fig-align: center
#| cache: true

ggplot(dat, aes(x = sed, fill = group)) +
    geom_density() +
    facet_grid(rows = vars(group)) +
    theme_cowplot() +
    theme(legend.position = "none") +
    xlab("Change in sed rate [mm/hr]\n(after - before)")
```

:::

::: {.fragment .fade-in-then-out}

Continuous probability distributions are typically visualized as smooth curves, *without bars*

![](/img/sed_den_area.png){width="80%" fig-align="center"}
:::

:::

# Break!

## Learning goals


1. Define [random trial]{.alert}, [event]{.alert}, and [outcome]{.alert} $\checkmark$
2. Define [probability]{.alert} $\checkmark$
3. Describe discrete and continuous [probability distributions]{.alert} $\checkmark$
4. Determine if events are [mutually exclusive]{.alert}
5. Determine if events are [independent]{.alert}

## [Probability]{.alert} recap

::: columns
::: {.column width="34%"}
The [random trial:]{.alert}

you roll the die

![](/img/die_roll.png){width="6cm"}
:::

::: {.column width="28%"}
An [event:]{.alert}

you get a 5

![](/img/5_die.png){width="2.5cm"}
:::

::: {.column width="38%"}
All possible [outcomes:]{.alert}

all integers 1---6
:::
:::

## [Probability]{.alert} recap

*The [probability]{.alert} of an event is the proportion of times that event would occur if we repeated a random trial over and over again under the same conditions. Probability always ranges between 0 and 1*

## [Probability]{.alert} recap

With a fair die, every outcome has equal probability:

```{r}
library(tidydice)
library(gridExtra)

d1 <- roll_dice(times = 1, prob = c(1, 0, 0, 0, 0, 0)) |>
    plot_dice(detailed = FALSE) + labs(title = NULL)
d2 <- roll_dice(times = 1, prob = c(0, 1, 0, 0, 0, 0)) |>
    plot_dice(detailed = FALSE) + labs(title = NULL)
d3 <- roll_dice(times = 1, prob = c(0, 0, 1, 0, 0, 0)) |>
    plot_dice(detailed = FALSE) + labs(title = NULL)
d4 <- roll_dice(times = 1, prob = c(0, 0, 0, 1, 0, 0)) |>
    plot_dice(detailed = FALSE) + labs(title = NULL)
d5 <- roll_dice(times = 1, prob = c(0, 0, 0, 0, 1, 0)) |>
    plot_dice(detailed = FALSE) + labs(title = NULL)
d6 <- roll_dice(times = 1, prob = c(0, 0, 0, 0, 0, 1), success = 1) |>
    plot_dice(detailed = FALSE) + labs(title = NULL)

pr <- data.frame(rep("= 1/6", 6))
names(pr) <- NULL
rownames(pr) <- paste0("Pr(rolling a ", 1:6, ")")

grid.arrange(d1, tableGrob(pr[1, ], theme = ttheme_default(base_size = 32)),
             d2, tableGrob(pr[2, ], theme = ttheme_default(base_size = 32)),
             d3, tableGrob(pr[3, ], theme = ttheme_default(base_size = 32)),
             d4, tableGrob(pr[4, ], theme = ttheme_default(base_size = 32)),
             d5, tableGrob(pr[5, ], theme = ttheme_default(base_size = 32)),
             d6, tableGrob(pr[6, ], theme = ttheme_default(base_size = 32)),
             ncol = 2)
```


## [Discrete probability distribution]{.alert}

![](/img/2_dice_hist.png){width="70%" fig-align="center"}

This histogram-looking thing is called a [discrete probability distribution]{.alert}

## [Continuous probability distribution]{.alert}

::: columns
::: {.column width="60%"}

Suppose we run a clinical study of the effect of noni juice on inflammatory arthritis. We randomly assign half the patients to receive 10mg noni extract and the other half to receive a placebo. We measure change in erythrocyte sedimentation rate (*sed rate*) in all patients before and after treatment (either 10mg noni or placebo)
:::

::: {.column width="40%"}
```{r}
#| fig-width: 4
#| fig-height: 4
#| fig-align: center
#| out-width: "5in"

set.seed(1)
dat <- data.frame(sed = c(rnorm(100, -2), rnorm(100)),
                  group = c(rep("10mg noni", 100),
                            rep("placebo", 100)))

ggplot(dat, aes(x = sed, fill = group)) +
    geom_histogram(aes(y = after_stat(density)), bins = 24) +
    facet_grid(rows = vars(group)) +
    theme_cowplot() +
    theme(legend.position = "none") +
    xlab("Change in sed rate [mm/hr]\n(after - before)")
```

:::
:::

::: {.fragment}
*Change in sed rate [mm/hr] is* ***not*** *an integer*

What do we do?!
:::

## [Continuous probability distribution]{.alert}

::: {.r-stack}

::: {.fragment .fade-in-then-out}
![](/img/sed_hist.png){width="80%" fig-align="center"}
:::

::: {.fragment}
Continuous probability distributions are typically visualized as smooth curves, *without bars*

![](/img/sed_den_area.png){width="80%" fig-align="center"}
:::

:::



## Connecting descriptions of data to probability

::: {.columns}

::: {.column width="60%"}

Let's plot the mean and SD for these data

```{r}
#| fig-width: 4
#| fig-height: 2.75
#| fig-align: center
#| out-width: "6in"

set.seed(1)
dat <- data.frame(sed = c(rnorm(100, -2), rnorm(100)),
                  group = c(rep("10mg noni", 100),
                            rep("placebo", 100)))

noni <- dat[dat$group != "placebo", ]
ybar <- mean(noni$sed)
s <- sd(noni$sed)

ann <- data.frame(x = ybar + c(0, 2 * s, -2 * s),
                  y = 0.5,
                  labels = c("bar(y)", "bar(y) + 2*s", "bar(y) - 2*s"))

p <- ggplot(noni, aes(x = sed, fill = group)) +
    geom_histogram(aes(y = after_stat(density)), bins = 20) +
    facet_grid(rows = vars(group)) +
    theme_cowplot() +
    theme(legend.position = "none") +
    geom_vline(xintercept = ybar) +
    geom_vline(xintercept = ybar + c(2 * s, -2 * s), linetype = 2) +
    xlab("Change in sed rate [mm/hr]\n(after - before)")

p + geom_label(data = ann, 
               aes(x = x, y = y, label = labels),
               nudge_x = c(0, 0.2, -0.2), 
               inherit.aes = FALSE, 
               parse = TRUE)

```

:::

::: {.column width="40%"}

::: {.fragment}

::: {.pause-think}
We say that 95% of the data fall within $\bar{y} \pm 2 s$, what does that mean in terms of probability?
:::

:::

:::

:::


## Connecting descriptions of data to probability



::: {.columns}

::: {.column width="60%"}

There is a 0.95 probability a randomly selected data point will be greater than $\bar{y} - 2 s$ and less than $\bar{y} + 2 s$

![](/img/brown_bag.png){width="30%" fig-align="center"}

"Put the data in a bag and shake it..."

:::

::: {.column width="40%"}

::: {.pause-think}
We say that 95% of the data fall within $\bar{y} \pm 2 s$, what does that mean in terms of probability?
:::


:::

:::



## Connecting descriptions of data to probability {.top-align}

::: {.columns}

::: {.column width="50%"}
Now let's plot the SE of the mean

::: {.r-stack}

:::: {.fragment .fade-in-then-out}
```{r}
#| fig-width: 4
#| fig-height: 2.75
#| fig-align: center
#| out-width: "6in"

p + geom_label(data = ann, 
               aes(x = x, y = y, label = labels),
               nudge_x = c(0, 0.2, -0.2), 
               inherit.aes = FALSE, 
               parse = TRUE)
```
::::


:::: {.fragment}

```{r}
#| fig-width: 4
#| fig-height: 2.75
#| fig-align: center
#| out-width: "6in"

se <- s / sqrt(nrow(noni))

ann$color <- "black"
ann <- rbind(ann,
             data.frame(x = ybar + 2 * c(-se, se),
                        y = c(0.3, 0.3),
                        labels = c("bar(y) - 2*SE", "bar(y) + 2*SE"),
                        color = "blue"))

p + annotate("rect", 
             xmin = ybar - 2 * se, xmax = ybar + 2 * se,
             ymin = -Inf, ymax = Inf,
             alpha = 0.5, 
             fill = "blue", 
             color = "blue", 
             linetype = 2) +
    geom_vline(xintercept = ybar) +
    geom_label(data = ann, 
               aes(x = x, y = y, label = labels),
               color = ann$color,
               nudge_x = c(0, 0.2, -0.2, -1.3, 1.3),
               hjust = c("middle", "middle", "middle", "left", "right"),
               inherit.aes = FALSE,
               parse = TRUE)


```
::::

:::

:::

::: {.column width="50%"}

::: {.fragment}
::: {.pause-think}
What does the interval from $\bar{y} - 2 SE$ to $\bar{y} + 2 SE$ mean in terms of probability?

::: {.fragment}
*Hint:* What goes in the bag?
:::

:::
:::

:::

:::

## Connecting descriptions of data to probability {.top-align}


::: {.columns}

::: {.column width="50%"}
What goes in the bag?


```{r}
#| fig-width: 4
#| fig-height: 2.75
#| fig-align: center
#| out-width: "6in"

p + annotate("rect", 
             xmin = ybar - 2 * se, xmax = ybar + 2 * se,
             ymin = -Inf, ymax = Inf,
             alpha = 0.5, 
             fill = "blue", 
             color = "blue", 
             linetype = 2) +
    geom_vline(xintercept = ybar) +
    geom_label(data = ann, 
               aes(x = x, y = y, label = labels),
               color = ann$color,
               nudge_x = c(0, 0.2, -0.2, -1.3, 1.3),
               hjust = c("middle", "middle", "middle", "left", "right"),
               inherit.aes = FALSE,
               parse = TRUE)


```


:::

::: {.column width="50%"}


<!-- ::: {.fragment} -->


::: {.r-stack}


![](/img/brown_bag.png){width="45%" fig-align="center"}

::: {.fragment}

We are dealing with the [sampling distribution]{.alert}

```{r}
#| fig-width: 3
#| fig-height: 2
#| fig-align: center
#| out-width: "4in"
#| dev.args: !expr list(bg = "transparent")

ggplot(data.frame(x = ybar + 3.75 * c(-se, se)), aes(x)) +
    stat_function(fun = dnorm, args = list(mean = ybar, sd = se)) +
    theme_cowplot() +
    xlab("MEAN change in sed rate") +
    ylab("density") +
    stat_function(fun = dnorm,
                  args = list(mean = ybar, sd = se),
                  xlim = ybar + 2 * c(-se, se),
                  geom = "area", fill = "blue", alpha = 0.5) +
    geom_vline(xintercept = ybar + c(0, -2 * se, 2 * se),
               linetype = c(1, 2, 2), color = c("black", "blue", "blue")) +
    theme(plot.background = element_rect(fill = "transparent", color = NA),
          panel.background = element_rect(fill = "transparent", color = NA))
```


Infinite hypothetical sample mean [estimates]{.alert} go in the bag!
:::
:::
<!-- ::: -->

:::

:::





# Rest your brain {background-image="/img/molokai.jpg" background-size="cover"}


## Learning goals

4. Determine if events are [mutually exclusive]{.alert}
5. Determine if events are [independent]{.alert}

## Rules of probability

::: {.pause-think}

If 0.95 probability is contained in the [blue]{style="color: blue;"} region, how much is contained in the [orange]{style="color: orange;"} and [red]{style="color: red;"} regions combined?

:::

```{r fig.width=3, fig.height=2, fig.align='center'}
normArea <- ggplot(data.frame(x = ybar + 3.75 * c(-se, se)), aes(x)) +
    theme_cowplot() +
    xlab("MEAN change in sed rate") +
    ylab("density") +
    stat_function(fun = dnorm,
                  args = list(mean = ybar, sd = se),
                  xlim = ybar + 2 * c(-se, se),
                  geom = "area", fill = "blue", alpha = 0.5) +
    stat_function(fun = dnorm,
                  args = list(mean = ybar, sd = se),
                  xlim = ybar + c(-3.75 * se, -2 *se),
                  geom = "area", fill = "orange") +
    stat_function(fun = dnorm,
                  args = list(mean = ybar, sd = se),
                  xlim = ybar + c(2 * se, 3.75 *se),
                  geom = "area", fill = "red") +
    stat_function(fun = dnorm, args = list(mean = ybar, sd = se))

normArea
```

::: {.fragment}
[Answer:]{.alert} 0.025 + 0.025 = 0.05 probability...why?
:::

## Rules of probability {.top-align}

::: {.incremental}
1. Probability is always between 0 and 1
2. The sum of all the probabilities of all the possible outcomes always equals 1
3. We can *add* the probabilities of [mutually exclusive]{.alert} outcomes
:::

::: {.fragment}
```{r fig.width=3, fig.height=2, fig.align='center'}
normArea
```

the [orange]{style="color: orange;"} and [red]{style="color: red;"} regions are [mutually exclusive]{.alert}
:::

## Rules of probability {.top-align}

1. Probability is always between 0 and 1
2. The sum of all the probabilities of all the possible outcomes always equals 1
3. We can *add* the probabilities of [mutually exclusive]{.alert} events
4. We can *simply multiply* the probabilities of [independent]{.alert} events

::: {style="text-align: center;"}
![](/img/dice_3-5.png){width="4cm"}
:::

the event of the second die roll is [independent]{.alert} of the first die roll, so the probability of a 3 for the first and a 5 for the second is $\frac{1}{6} \times \frac{1}{6} = \frac{1}{36}$

## Rules of probability {.top-align}


Adding and multiplying probabilities have special vocab:

**Adding is an "OR" situation:**

> The mean is less than -2 OR greater than -1.7

$$Pr(\bar{y} < - 2) + Pr(\bar{y} > -1.7) = 0.05$$

## Rules of probability {.top-align}

Adding and multiplying probabilities have special vocab:

**Multiplying is an "AND" situation:**

> The first die is 3 AND the second is 5

$$Pr(\text{1}^{st} \text{ die} = 3) \times Pr(\text{2}^{nd} \text{ die} = 5) = \frac{1}{36}$$

## Rules of probability {.top-align}

Adding and multiplying probabilities have special vocab:

**Adding is an "OR" situation:**

> The mean is less than -2 OR greater than -1.7

$$Pr(\bar{y} < - 2) + Pr(\bar{y} > -1.7) = 0.05$$

> The first die is 3 OR the second is 5

$$Pr(\text{1}^{st} \text{ die} = 3) + Pr(\text{2}^{nd} \text{ die} = 5) = \frac{1}{3}$$

## Rules of probability {.top-align}

1. Probability is always between 0 and 1
2. The sum of all the probabilities of all the possible outcomes always equals 1
3. We can *add* the probabilities of [mutually exclusive]{.alert} events
4. We can *simply multiply* the probabilities of [independent]{.alert} events
5. We can *add* the probabilities of [independent]{.alert} events

## Rules of probability {.top-align}

1. Probability is always between 0 and 1
2. The sum of all the probabilities of all the possible outcomes always equals 1
3. We can *add* the probabilities of [mutually exclusive]{.alert} events
4. We can *simply multiply* the probabilities of [independent]{.alert} events
5. We can *add* the probabilities of [independent]{.alert} events
5. What about [non-independent]{.alert} probabilities???



## Non-independent events

You go catch a fish and...

::: columns
::: {.column width="50%"}
::: {style="text-align: center;"}
*A*
:::
:::

::: {.column width="50%"}
::: {style="text-align: center;"}
*B*
:::
:::
:::

::: columns
::: {.column width="50%"}
**Length is greater than 50 cm**
:::

::: {.column width="50%"}
**It's an uhu**

![](/img/uhu.png)
:::
:::

## Non-independent events

```{r}
#| fig-align: center
#| fig-width: 6
#| fig-height: 4.5
#| out-width: "8in"

library(sf)

sfcirc <- function(x, y, r) {
    st_point(c(x, y)) |>
        st_sfc() |>
        st_sf(geometry = _) |>
        st_buffer(dist = r)
}

A <- sfcirc(-1, 0, 2)
B <- sfcirc(1, 0, 2)

AandB <- st_intersection(A, B)
AorB <- st_union(A, B)
Aonly <- st_difference(A, B)
Bonly <- st_difference(B, A)

Acol <- hsv(0.6, 1, 0.8)
Afill <- hsv(0.6, 0.5)
Bcol <- hsv(1, 0.7, 0.7)
Bfill <- hsv(1, 0.4, 0.9)
ABcol <- hsv(0.8, 1, 0.5)
ABfill <- hsv(0.8, 0.7, 0.7)

prob_venn <- ggplot() +
    annotate("rect", xmin = -4, xmax = 4, ymin = -3, ymax = 3,
             fill = "gray85", color = "black") +
    annotate("text", x = 0, y = -2.5,
             label = expression("not (A OR B) = ("*l <= 50*"cm & not uhu)"),
             size = 8) +
    geom_sf(data = B, fill = Bfill, color = Bcol, linewidth = 1) +
    geom_sf(data = A, fill = Afill, color = Acol, linewidth = 1) +
    geom_sf(data = AandB, fill = ABfill, color = ABcol, linewidth = 1) +
    annotate("text", x = 2, y = 1, label = "B", color = Bcol, size = 10) +
    annotate("text", x = 2, y = 0.5, label = "uhu", color = Bcol, size = 6) +
    annotate("text", x = 2, y = 0, label = "0.3", color = Bcol, size = 10) +
    annotate("text", x = 0, y = 0.75, label = "A&B", color = ABcol, size = 10) +
    annotate("text", x = 0, y = 0, label = "0.1", color = ABcol, size = 10) +
    annotate("text", x = -2, y = 1, label = "A", color = Acol, size = 10) +
    annotate("text", x = -2, y = 0.5, label = "l > 50 cm", color = Acol,
             size = 6) +
    annotate("text", x = -2, y = 0, label = "0.2", color = Acol, size = 10) +
    xlim(c(-4, 4)) + ylim(c(-3, 3)) +
    theme_nothing()

prob_venn
```

## Non-independent events


::: columns
::: {.column width="65%"}
```{r}
#| fig-align: center
#| fig-width: 6
#| fig-height: 4.5
#| out-width: "8in"

prob_venn
```
:::

::: {.column width="35%"}
**Calculating probabilities**

$Pr(A) = 0.2$

$Pr(B) = 0.3$

$Pr(A~\&~B) = 0.1$

$Pr(A~OR~B) = \text{??}$
:::
:::

## Non-independent events {.top-align}

::: columns
::: {.column width="40%"}
```{r}
#| fig-align: center
#| fig-width: 6
#| fig-height: 4.5
#| out-width: "8in"

prob_venn + geom_sf(data = AorB, fill = "black") +
    annotate("text", x = 0, y = 0, label = "A OR B", color = "white",
             size = 10) +
    annotate("rect", xmin = -3.9, xmax = 3.9, ymin = -2.9, ymax = -2.1,
             fill = "gray85", color = "gray85")
```


:::

::: {.column width="60%"}
**Calculating probabilities**

$Pr(A~OR~B) \neq Pr(A) + Pr(B)$

<br>

Why?


:::
:::

## Non-independent events {.top-align}

::: columns
::: {.column width="40%"}
```{r}
#| fig-align: center
#| fig-width: 6
#| fig-height: 4.5
#| out-width: "8in"

prob_venn + geom_sf(data = AorB, fill = "black") +
    annotate("text", x = 0, y = 0, label = "A OR B", color = "white",
             size = 10) +
    annotate("rect", xmin = -3.9, xmax = 3.9, ymin = -2.9, ymax = -2.1,
             fill = "gray85", color = "gray85")
```

```{r}
#| fig-align: center
#| fig-width: 6
#| fig-height: 4.5
#| out-width: "8in"

prA <- ggplot() +
    geom_sf(data = A, fill = Afill, color = Acol, linewidth = 1) +
    geom_sf(data = AandB, fill = ABfill, color = ABcol, linewidth = 1) +
    annotate("text", x = 0, y = 0.75, label = "A&B", color = ABcol, size = 10) +
    annotate("text", x = -2, y = 1, label = "A", color = Acol, size = 10) +
    annotate("text", x = -2, y = 0.2, label = "0.2", color = Acol, size = 10) +
    annotate("text", x = 0, y = 0, label = "0.1", color = ABcol, size = 10) +
    theme_nothing()

prB <- ggplot() +
    geom_sf(data = B, fill = Bfill, color = Bcol, linewidth = 1) +
    geom_sf(data = AandB, fill = ABfill, color = ABcol, linewidth = 1) +
    annotate("text", x = 2, y = 1, label = "B", color = Bcol, size = 10) +
    annotate("text", x = 0, y = 0.75, label = "A&B", color = ABcol, size = 10) +
    annotate("text", x = 2, y = 0, label = "0.3", color = Bcol, size = 10) +
    annotate("text", x = 0, y = 0, label = "0.1", color = ABcol, size = 10) +
    theme_nothing()

plot_grid(prA, prB, nrow = 1)
```

:::

::: {.column width="60%"}
**Calculating probabilities**

$Pr(A~OR~B) \neq Pr(A) + Pr(B)$

<br>

Why? Double counts $Pr(A~\&~B)$

<br>


::: {.fragment}
$Pr(A~OR~B) = Pr(A) + Pr(B) - Pr(A~\&~B)$
:::

::: {.fragment}
$$
\begin{align}
\Rightarrow Pr(A~OR~B) &= 0.2 + 0.3 - 0.1 \\
&= 0.4
\end{align}
$$
:::

:::
:::

## Non-independent events

::: columns
::: {.column width="25%"}
```{r}
#| fig-align: center
#| fig-width: 2
#| fig-height: 2

prA
```
:::

::: {.column width="75%"}
**Calculating probabilities**

$Pr(B \mid A)$

"The probability of observing $B$ given that we already know $A$ happened"

::: {.fragment}
$Pr(B \mid A) = \frac{Pr(A~\&~B)}{Pr(A)}$
:::

::: {.fragment}
The [event]{.alert} is [$\mathbf{(A~\&~B)}$]{style="color: purple;"} but the possible [outcomes]{.alert} are limited to [$\mathbf{(A~\&~B)}$]{style="color: purple;"} or [$\mathbf{(A~NOT~B)}$]{style="color: blue;"}.
:::


::: {.fragment}
And $Pr(A) = Pr(A~\&~B) + Pr(A~NOT~B)$
:::

::: {.fragment}
so we have to normalize the probability of [$\mathbf{(A~\&~B)}$]{style="color: purple;"} by the probability of $\mathbf{(A)}$
:::

:::
:::

## Non-independent events

::: columns
::: {.column width="40%"}
```{r}
#| fig-align: center
#| fig-width: 6
#| fig-height: 4.5
#| out-width: "8in"

prob_venn
```
:::



::: {.column width="60%"}
**Calculating probabilities**

Note that we can re-arrange this equation

$Pr(B \mid A) = \frac{Pr(A~\&~B)}{Pr(A)}$

::: {.fragment}
$Pr(B \mid A) Pr(A) = Pr(A~\&~B)$
:::

<br>

::: {.fragment}
By extension, this equation is also true

$Pr(A \mid B) Pr(B) = Pr(A~\&~B)$
:::

<br>

::: {.fragment}
Therefore

$Pr(A \mid B) Pr(B) = Pr(B \mid A) Pr(A)$
:::

:::
:::



## Non-independent events

$Pr(A \mid B) Pr(B) = Pr(B \mid A) Pr(A)$

::: {.fragment}
And re-arranging

$$Pr(A \mid B)  = \frac{Pr(B \mid A) Pr(A)}{Pr(B)}$$
:::

::: {.fragment}
**This is Bayes' Theorem** and it is super useful
:::

## Rules of probability

1. Probability is always between 0 and 1
2. The sum of all the probabilities of all the possible outcomes always equals 1
3. We can *add* the probabilities of [mutually exclusive]{.alert} events
5. We can *add* the probabilities of [independent]{.alert} events
4. We can *simply multiply* the probabilities of [independent]{.alert} events

For [non-independent]{.alert} events (but also works for independent events):

6. $Pr(A~OR~B) = Pr(A) + Pr(B) - Pr(A~\&~B)$
7. $Pr(A~\&~B) = Pr(A | B) Pr(B)$
8. Bayes' theorem: $Pr(A | B) = \frac{Pr(B | A) Pr(A)}{Pr(B)}$
